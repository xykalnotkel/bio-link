import AdminPanel from "./AdminPanel";

export const dynamic = "force-dynamic";

export const metadata = { title: "Panel Admin — Bio Link" };

export default function AdminPage() {
  return <AdminPanel />;
}
